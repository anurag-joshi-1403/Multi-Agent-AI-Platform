# Test classpath shadows src/main/resources/application.properties.
# Bean construction needs *a* key; no test may call the real API with it.
spring.ai.model.chat=google-genai
spring.ai.google.genai.api-key=test-key-not-used
spring.ai.google.genai.chat.options.model=gemini-2.5-flash
spring.ai.anthropic.api-key=test-key-not-used
spring.ai.openai.api-key=test-key-not-used

spring.ai.model.embedding=none
spring.ai.model.image=none
spring.ai.model.audio.speech=none
spring.ai.model.audio.transcription=none
spring.ai.model.moderation=none

spring.mvc.problemdetails.enabled=true

# Persistence runs against in-memory H2 rather than the Postgres from compose.yaml, so `./mvnw test`
# still needs no Docker and no network. The JDBC code under test is the same code that runs in
# production - only the dialect differs, and Spring AI ships an H2 chat-memory dialect alongside its
# Postgres one. DB_CLOSE_DELAY=-1 keeps the database alive between connections within a test.
spring.docker.compose.enabled=false
spring.datasource.url=jdbc:h2:mem:platform;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.sql.init.mode=always
spring.ai.chat.memory.repository.jdbc.initialize-schema=always
